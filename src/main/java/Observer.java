/**
 * This class acts as a base observer class to be used by every observer as required by the observer pattern.
 * It contains the methods to update the observer.
 * @author asuender
 * @version 0.2
 */
public interface Observer {
    public void update();
}
